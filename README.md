- 新增按患者及序列分组的 K 折训练，避免相邻时相数据跨训练集与验证集造成数据泄漏。
  - 新增多模型概率集成、2.5D 输入及 DIAS 目标域平衡采样，提升血管分割效果和模型泛化能力。其中2.5D理论上应该只用在同一患者的连续图像中。
  - 优化验证集阈值扫描，支持先粗扫再局部精扫，并修复可能的扫描失败。
  - 优化 DataLoader 数据读取速度，加入常驻 Worker、批次预取和每 Worker 解码缓存，可以有效提高训练速度；这些优化同样适用于项目中的其他模型。
  - 上传三折训练结果 test3，最佳验证 Dice 分别为 0.7920、0.7824 和 0.7601（在k折验证集内部），用dias验证集测试时略有下降，但也能达到0.76~0.78左右，可用于单模型推理或三折概率集成
  - 中间尝试了frangi，效果极不理想，可忽略。
